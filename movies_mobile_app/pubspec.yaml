name: patcharaphorn_final_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.8.1  # Change this to match firebase_auth version
  firebase_auth: ^5.3.4  # Keep this as it is
  cloud_firestore: ^5.0.0  # Firebase Firestore dependency
  cached_network_image: ^3.2.1
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true


